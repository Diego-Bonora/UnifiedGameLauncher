import { APP_NAME } from '@shared/app-info'

function App(): React.JSX.Element {
  return (
    <main>
      <h1>{APP_NAME}</h1>
      <p>Your library will live here.</p>
    </main>
  )
}

export default App
